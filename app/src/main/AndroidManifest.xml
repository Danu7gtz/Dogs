<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.dogs" xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_dog_two"
        android:label="@string/app_name"
        android:theme="@style/Theme.Dogs">

        <!-- No tiene intent-filters, puede ir exported=false (o simplemente omitirlo) -->
        <activity
            android:name=".ui.AgendaActivity"
            android:exported="false" />

        <!-- Tiene MAIN/LAUNCHER, por eso debe ir exported=true -->
        <activity
            android:name=".ui.LoginActivity"
            android:windowSoftInputMode="stateUnchanged|adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>
</manifest>


